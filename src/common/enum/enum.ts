export enum StatusEnum {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  DONE = 'done',
}

export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
}
  
export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

